# Additional dependencies for training voice biometric models
# Install with: pip install -r requirements.txt -r training_requirements.txt

# Training-specific packages not in main requirements
datasets>=2.14.0
tqdm>=4.64.0
hyperpyyaml>=1.2.0
matplotlib>=3.6.0
seaborn>=0.12.0

# Note: Main ML dependencies (torch, librosa, scipy, etc.) are in requirements.txt